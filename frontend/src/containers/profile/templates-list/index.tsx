import React from 'react';

import { TemplatesListContainer } from './container';

// страница списка шаблонов
const TemplatesList = (props: any) => {
  return <TemplatesListContainer {...props} />;
};

export default TemplatesList;
